Base image for NodeJS applications.

Examples:
- Run this image to review NodeJS installation and settings:
> docker run -it -e TERM="${TERM}" -e TERMCAP="${TERMCAP}" ${NAMESPACE}/${IMAGE}:${TAG} /bin/bash
